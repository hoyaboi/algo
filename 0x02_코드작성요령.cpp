#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);    // 기본적으로 c++과 c의 stream을 동기화 -> 시간 단축 위해 동기화 끊는 코드 
                                    // 동기화 끊고 c stream(printf, scanf) 사용하면 안됨!!

    cin.tie(nullptr);               // cin 수행 전 cout 버퍼를 비우지 않는 코드
                                    // 기본적으로 cin을 수행하기 전에 cout 버퍼를 비워줌(사용자 입장에서 편하게)
                                    // 코딩 테스트에서 채점시 출력 글자만 확인 -> 굳이 출력 버퍼를 비울 필요x(시간 단축)
    // endl 사용 X!! -> endl은 개행문자(\n)을 출력하고 출력 버퍼를 비우라는 명령 -> 위에 설명대로 굳이 비울 필요 x
    

}
